{
  "name": "kbp4-editor-monorepo",
  "version": "1.4.0",
  "private": true,
  "license": "MPL-2.0",
  "workspaces": [
    "apps/web",
    "apps/desktop",
    "packages/kbp4-js-api"
  ],
  "scripts": {
    "deploy:web": "npm run deploy --workspace apps/web --",
    "deploy:web:dev": "npm run deploy:dev --workspace apps/web --",
    "build:deploy:web": "npm run build:deploy --workspace apps/web --",
    "build:deploy:web:dev": "npm run build:deploy:dev --workspace apps/web --"
  },
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=10.0.0"
  }
}
